service: lambda-relatorio

frameworkVersion: '3'

provider:
  name: aws
  runtime: java25
  region: us-east-2

package:
  artifact: target/function.zip

functions:
  relatorio:
    handler: io.quarkus.amazon.lambda.runtime.QuarkusStreamHandler::handleRequest
    events:
      - schedule:
          rate: cron(0 8 ? * MON *)
          enabled: true
          description: Dispara relatorio semanal toda segunda-feira as 08:00 UTC
